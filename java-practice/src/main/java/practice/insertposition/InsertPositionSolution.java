package practice.insertposition;

/**
 * Interface for InsertPosition challenge implementations.
 * All attempts (InsertPosition, InsertPosition2, etc.) should implement this.
 */
public interface InsertPositionSolution {
    int searchInsert(int[] nums, int target);
}
