package practice.issorted;

/**
 * Interface for IsSorted challenge implementations.
 * All attempts (IsSorted, IsSorted2, etc.) should implement this.
 */
public interface IsSortedSolution {
    boolean checkSort(int[] arr);
}
