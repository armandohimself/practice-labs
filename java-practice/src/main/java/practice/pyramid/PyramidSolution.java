package practice.pyramid;

/**
 * Interface for Pyramid challenge implementations.
 * All attempts (Pyramid, Pyramid2, etc.) should implement this.
 */
public interface PyramidSolution {
    String returnPyramid(int n);
}
