package practice.indexof;

/**
 * Interface for IndexOf challenge implementations.
 * All attempts (IndexOf, IndexOf2, etc.) should implement this.
 */
public interface IndexOfSolution {
    int getIndex(int[] arr, int n);
}
