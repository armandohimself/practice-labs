package practice.istheresum;

/**
 * Interface for IsThereSum challenge implementations.
 * All attempts (IsThereSum, IsThereSum2, etc.) should implement this.
 */
public interface IsThereSumSolution {
    boolean check(int[] arr, int target);
}
