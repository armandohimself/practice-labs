package practice.arrayabsolutevalue;

/**
 * Interface for ArrayAbsoluteValue challenge implementations.
 * All attempts (ArrayAbsoluteValue, ArrayAbsoluteValue2, etc.) should implement this.
 */
public interface ArrayAbsoluteValueSolution {
    int[] getArrayAbs(int[] nums);
}
