package practice.leetcode.arrays;

/**
 * Container With Most Water (LeetCode #11)
 *
 * You are given an integer array height of length n. There are n vertical lines drawn such that
 * the two endpoints of the ith line are (i, 0) and (i, height[i]).
 * Find two lines that together with the x-axis form a container, such that the container contains
 * the most water. Return the maximum amount of water a container can store.
 *
 * Example 1:
 *   Input: height = [1,8,6,2,5,4,8,3,7]
 *   Output: 49
 *   Explanation: The vertical lines are represented by array [1,8,6,2,5,4,8,3,7].
 *   The max area of water is between index 1 and 8, area = min(8,7) * (8-1) = 7 * 7 = 49.
 *
 * Example 2:
 *   Input: height = [1,1]
 *   Output: 1
 */
public class ContainerWithMostWater {

    /**
     * Finds the maximum area of water that can be contained.
     *
     * @param height array of heights
     * @return maximum water area
     */
    public int maxArea(int[] height) {
        // TODO: Implement your solution here (consider two pointers)
        return 0;
    }
}
