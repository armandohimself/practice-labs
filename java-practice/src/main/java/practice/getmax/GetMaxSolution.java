package practice.getmax;

/**
 * Interface for GetMax challenge implementations.
 * All attempts (GetMax, GetMax2, etc.) should implement this.
 */
public interface GetMaxSolution {
    int max(int[] arr);
}
