package practice.subarray;

/**
 * Interface for SubArray challenge implementations.
 * All attempts (SubArray, SubArray2, etc.) should implement this.
 */
public interface SubArraySolution {
    int[] sub(int[] nums, int start, int end);
}
