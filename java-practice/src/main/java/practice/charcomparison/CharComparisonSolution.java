package practice.charcomparison;

/**
 * Interface for CharComparison challenge implementations.
 * All attempts (CharComparison, CharComparison2, etc.) should implement this.
 */
public interface CharComparisonSolution {
    int compare(char[] a, char[] b);
}
