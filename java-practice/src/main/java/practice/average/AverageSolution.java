package practice.average;

/**
 * Interface for Average challenge implementations.
 * All attempts (Average, Average2, etc.) should implement this.
 */
public interface AverageSolution {
    double getAvg(int[] nums);
}
